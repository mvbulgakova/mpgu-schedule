# Чек-лист проверки качества парсинга расписания МПГУ

Проводится после каждого запуска скрейпера или при подозрении на проблему с данными.

---

## 1. Полнота данных по институтам

- [ ] **1.1** Все 17 институтов присутствуют в `meta/index.json`
- [ ] **1.2** У всех институтов `status == "ok"` (нет записей со статусом `error`)
- [ ] **1.3** Количество групп не меньше минимального порога:
  - Крупные институты (ИФил, ИМО, ИЖКМ, ИД, ИИиП): ≥ 40 групп
  - Средние (ИБиХ, ИФКСиЗ, ИПиП, ГФ, ИМиИ): ≥ 15 групп
  - Малые (ИФТИС, ИРЦО, ИПРП, ФДПиП): ≥ 2 групп
  - **ИИЯ: ≥ 60 групп** (сейчас 2 — критическая ошибка парсинга)
- [ ] **1.4** `updated_at` у каждого института не старше 3 дней от текущей даты
- [ ] **1.5** Все 17 институтов имеют директорию `groups/` в ветке `data`

---

## 2. Мусорные имена групп

Для каждого института проверить: нет ли групп с именами из следующих категорий:

- [ ] **2.1** Заголовки разделов расписания:
  - Слова: `магистратура`, `бакалавриат`, `расписание`, `курс`, `семестр`, `направление`, `профиль`, `кафедра`, `форма обучения`
  - Примеры найденных: ИМО — "Магистратура 1 курс", "История России", "Государственный"
- [ ] **2.2** Фамилия преподавателя вместо кода группы (все буквы заглавные, нет цифр)
  - Пример: ИМО — "ГОРДЕЕВА"
- [ ] **2.3** Полное название специальности / направления подготовки
  - Пример: ИМО — "Педагогическое образование (профиль: Компьютерные технологии...)"
- [ ] **2.4** Голые дефолты: `группа`, `группа N` (без реального кода)
  - Найдены в: ИИИ, ИД
- [ ] **2.5** Только цифры: `201`, `202` и т.п. — не являются кодом группы
- [ ] **2.6** Код специальности вида `44.03.01`, `09.03.02` и т.п.
- [ ] **2.7** День недели: `СУББОТА`, `понедельник` и т.п.
- [ ] **2.8** Пробел внутри кода группы, разрывающий его (`БОЛ01 5-ГДР2201` вместо `БОЛОУС-...`)
  - Найдено в ИМО: 3 группы — OCR-ошибка, требует ручной правки в источнике

---

## 3. Полнота групп внутри института

- [ ] **3.1** Присутствуют группы всех курсов (1–4 для бакалавриата, 1–2 для магистратуры)
  - Проверить поле `year` в манифесте — нет ли пропущенных лет
- [ ] **3.2** Присутствуют все ожидаемые формы обучения (`full_time`, `correspondence`) там, где они должны быть
- [ ] **3.3** Нет пропущенных кафедр / направлений:
  - **ИИИ: нет групп худграфа** (коды ХГ / ХГФ — художественно-графический факультет)
  - При появлении новых пропусков сверяться с официальным сайтом МПГУ
- [ ] **3.4** Нет дублей группы с разным регистром или лишними пробелами (отсортировать имена и просмотреть)

---

## 4. Качество занятий внутри группы

Для выборки групп (минимум 2–3 на институт, в т.ч. проблемные):

- [ ] **4.1** `subject` не пустой, не числовой и не является техническим артефактом
- [ ] **4.2** `time_start` и `time_end` в формате `HH:MM`, время окончания позже времени начала
- [ ] **4.3** `slot` — целое число от 1 до 8 или `null`
- [ ] **4.4** `type` — одно из: `lecture`, `practice`, `lab`, `seminar`, `other`
- [ ] **4.5** `room` не содержит имя преподавателя (и наоборот — `teacher` не содержит номер аудитории)
- [ ] **4.6** Группа не полностью пустая (суммарно ≥ 1 занятие по всем дням и неделям)
- [ ] **4.7** Группа не слишком разреженная: < 3 занятий суммарно — подозрительно, нужна ручная проверка

---

## 5. Ручное сравнение с реальным расписанием

Наиболее важная проверка: сравниваем то, что мы распарсили, с тем, что показывает официальный сайт МПГУ или исходный PDF/Excel.

### Как проверять

1. Выбрать 3–5 групп из разных институтов (обязательно включить ранее проблемные: ИМО, ИИЯ, ИИИ)
2. Открыть исходный документ расписания для группы (PDF / Excel / Nextcloud)
3. Открыть `institutes/{id}/groups/{filename}.json` в ветке `data`
4. Сравнить по каждому дню:

| Что сверяем | На что смотреть |
|-------------|-----------------|
| Список предметов | Все ли предметы попали, нет ли лишних |
| Время начала/конца пары | Совпадает ли точно |
| Номер пары (slot) | Соответствует ли расписанию звонков МПГУ |
| Преподаватель | Корректно ли разобрано ФИО (не "А.С." вместо "А.С. Иванов") |
| Аудитория | Не перепутана ли с преподавателем |
| Чётная/нечётная неделя | Занятия на правильной неделе |
| Подгруппы | Подгруппа 1 и 2 не перемешаны |

### Группы для обязательной ручной проверки при каждом запуске

- [ ] **5.1** ИИЯ: любая группа (институт системно недопарсивается)
- [ ] **5.2** ИМО: 2 группы — одна с нормальным кодом, одна подозрительная
- [ ] **5.3** ИИИ: группа из направления, где предположительно должен быть худграф
- [ ] **5.4** ИФил / ИИиП: по 1 группе (крупные институты, много данных)
- [ ] **5.5** Любая группа заочной формы (correspondence) — они часто парсятся хуже
- [ ] **5.6** Любая магистерская группа (year = 1 или 2, degree = master)

### Документирование расхождений

При обнаружении расхождения зафиксировать:
```
Институт: ...
Группа: ...
Источник: [URL / имя файла]
День и неделя: [напр. понедельник, нечётная]
Проблема: [пара пропущена / время неверное / преподаватель не тот / ...]
```

---

## 6. Данные преподавателей

- [ ] **6.1** `meta/teachers.json` существует, `count` > 0
- [ ] **6.2** У преподавателей с `has_schedule: true` существует файл `teachers/{slug}.json`
- [ ] **6.3** Нет преподавателей с аббревиатурой вида `А.С.` — только инициалы без фамилии
- [ ] **6.4** Нет дублирующихся `staff_slug`
- [ ] **6.5** Расписание преподавателя совпадает с его занятиями в группах (выборочно 2–3 чел.)

---

## 7. Технические проверки

- [ ] **7.1** Все файлы `groups/*.json` корректный JSON (не обрезанный, не пустой объект `{}`)
- [ ] **7.2** `schedule.json` каждого института содержит корректный манифест (`groups` — массив объектов с полями `name`, `file`, `year`, `form`, `degree`)
- [ ] **7.3** Для каждой записи в манифесте существует соответствующий файл `groups/{file}.json`
- [ ] **7.4** jsDelivr отдаёт свежие данные: проверить заголовок `Last-Modified` на нескольких файлах (CDN обновляется через ~5 минут после пуша в ветку `data`)

---

## 8. Известные открытые проблемы

| Институт | Проблема | Статус |
|----------|----------|--------|
| ИИЯ | 2 группы вместо ~60–80 | 🔴 Не исправлено |
| ИМО | 12 мусорных групп (заголовки, ФИО, описания) | 🔴 Не исправлено |
| ИМО | 3 группы с пробелом в коде (`БОЛ01 5-...`) | 🟠 Не исправлено |
| ИИИ | Нет групп худграфа | 🟠 Не исправлено |
| ИИИ, ИД | Группа с именем `группа` | 🟡 Не исправлено |
| ИПиП | "Магистратура, направление 37.04.01..." | 🟡 Не исправлено |
